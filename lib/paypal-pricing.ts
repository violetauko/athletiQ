/** Athlete registration fee when paying with PayPal only (KES). Other methods use `NEXT_PUBLIC_ENTRY_FEE_AMOUNT`. */
export const PAYPAL_REGISTRATION_FEE_KES = 1200

/** Added once per marketplace order when checking out with PayPal (KES). */
export const PAYPAL_MARKETPLACE_SURCHARGE_KES = 200
